# Cursor Adapter

Cursor uses DevKit through project rules in `.cursor/rules/`.

## Install

```bash
scripts/install.sh --agent cursor --agent-target /path/to/project
```

This installs `.cursor/rules/devkit.mdc` and DevKit docs under `devkit/` in the target project.

