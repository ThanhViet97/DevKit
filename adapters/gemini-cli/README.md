# Gemini CLI Adapter

Gemini CLI uses DevKit through a project `GEMINI.md` context file.

## Install

```bash
scripts/install.sh --agent gemini-cli --agent-target /path/to/project
```

This installs `GEMINI.md` and DevKit docs under `devkit/` in the target project.

