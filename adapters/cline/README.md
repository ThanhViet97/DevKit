# Cline Adapter

Cline uses DevKit through workspace rules in `.clinerules/`.

## Install

```bash
scripts/install.sh --agent cline --agent-target /path/to/project
```

This installs `.clinerules/devkit.md` and DevKit docs under `devkit/` in the target project.

